```
sudo pacman -S --needed python-pipx && \
pipx ensurepath && \
source ~/.bashrc && \
pipx install konsave && \
wget -O nonaka.knsv "https://github.com/NonakaVal/dotfiles/raw/refs/heads/main/nonaka.knsv"
```

```
sudo apt update && sudo apt install -y python3-pip pipx wget && \
pipx ensurepath && \
source ~/.bashrc && \
pipx install konsave && \
wget -O nonaka.knsv "https://github.com/NonakaVal/dotfiles/raw/refs/heads/main/nonaka.knsv"
```
